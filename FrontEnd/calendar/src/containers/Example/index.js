import React from "react";

const styles = {
    pageWrapper: {
        padding: 20,
    },
};

const Example = () => {
    return (
        <div style={styles.pageWrapper}>
            <h1>Example</h1>
        </div>
    );
};

export default Example;
